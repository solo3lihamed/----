# دليل الاستخدام السريع - موقع عَبَق

## 🚀 تشغيل الموقع

### 1. تفعيل البيئة الافتراضية
```bash
source venv/bin/activate
```

### 2. تشغيل السيرفر
```bash
python manage.py runserver
```

### 3. فتح الموقع
افتح المتصفح واذهب إلى: http://localhost:8000/

## 👨‍💼 الوصول للوحة الإدارة

### رابط لوحة الإدارة
http://localhost:8000/admin/

### بيانات تسجيل الدخول
- **اسم المستخدم**: admin
- **كلمة المرور**: admin123

## 📋 الوظائف الأساسية

### للأدمن (من لوحة التحكم):

1. **إدارة المنتجات**:
   - إضافة عطور جديدة مع الصور والأسعار
   - تعديل المنتجات الموجودة
   - تحديد المنتجات المميزة

2. **إدارة الطلبات**:
   - عرض جميع الطلبات
   - تحديث حالة الطلبات (6 مراحل)
   - عرض تفاصيل كل طلب

3. **إدارة الفئات والعلامات**:
   - إضافة فئات جديدة مع أيقوناتها
   - إدارة العلامات التجارية

### للعملاء (من الموقع):

1. **التسوق**:
   - تصفح العطور حسب الفئات
   - البحث والفلترة حسب السعر
   - إضافة المنتجات للسلة

2. **إتمام الطلب**:
   - إدخال بيانات التواصل (الاسم، الهاتف، العنوان)
   - الحصول على رمز تتبع فريد

3. **التتبع**:
   - إدخال رمز التتبع
   - متابعة حالة الشحنة عبر 6 مراحل

4. **الحساب**:
   - إنشاء حساب جديد
   - تسجيل الدخول
   - عرض الطلبات السابقة

## 🌍 تغيير اللغة

يمكن التبديل بين العربية والإنجليزية من:
- أيقونة اللغة في الهيدر (🌐)
- يتم حفظ اختيار اللغة تلقائياً

## 📦 المراحل الست للشحنة

1. **قيد المراجعة** - طلب جديد تم استلامه
2. **تم التأكيد** - تم تأكيد الطلب من الأدمن
3. **قيد التحضير** - جاري تحضير المنتجات
4. **تم الشحن** - تم شحن الطلب
5. **في الطريق** - الطلب في الطريق للعميل
6. **تم التسليم** - تم تسليم الطلب بنجاح

## 🎯 نصائح مهمة

### للأدمن:
- عند تغيير حالة الطلب، يتم إنشاء سجل تتبع تلقائياً
- يمكن إضافة ملاحظات لكل تحديث للحالة
- احرص على تحديث صور المنتجات بصور عالية الجودة

### للعملاء:
- احفظ رمز التتبع في مكان آمن
- يمكن تتبع الشحنة في أي وقت باستخدام الرمز
- البيانات المدخلة آمنة ومحمية

## 🛠️ أوامر مفيدة

### إنشاء superuser جديد
```bash
python manage.py createsuperuser
```

### جمع الملفات الثابتة
```bash
python manage.py collectstatic
```

### إنشاء migrations
```bash
python manage.py makemigrations
```

### تطبيق migrations
```bash
python manage.py migrate
```

### إضافة بيانات تجريبية
```bash
python add_sample_data.py
```

## 📱 الصفحات الرئيسية

| الصفحة | الرابط |
|--------|--------|
| الرئيسية | / |
| العطور | /perfumes/ |
| السلة | /cart/ |
| إتمام الطلب | /orders/create/ |
| التتبع | /tracking/ |
| طلباتي | /orders/my-orders/ |
| تسجيل دخول | /login/ |
| تسجيل جديد | /register/ |
| لوحة الإدارة | /admin/ |

## 🎨 التخصيص

### تغيير الألوان
عدّل الألوان في `templates/base.html` في قسم الـ CSS

### إضافة صور المنتجات
1. اذهب للوحة الإدارة
2. اختر المنتج
3. ارفع الصورة الرئيسية وصور إضافية

### تعديل معلومات التواصل
عدّل معلومات التواصل في `templates/base.html` في قسم الفوتر

## ❓ حل المشاكل الشائعة

### الموقع لا يعمل
```bash
# تحقق من الأخطاء
python manage.py check

# تحقق من وجود migrations
python manage.py showmigrations
```

### الصور لا تظهر
```bash
# تحقق من إعدادات MEDIA في settings.py
# تأكد من رفع الصور عبر لوحة الإدارة
```

### الترجمة لا تعمل
```bash
# تحقق من إعدادات اللغة في settings.py
# تأكد من تفعيل LocaleMiddleware
```

## 📞 الدعم

للمزيد من المساعدة، راجع ملف README.md الرئيسي.

---

<div align="center">
  <p>صُنع بـ ❤️ لعشاق العطور الفاخرة</p>
  <p>© 2024 عَبَق - Abaq</p>
</div>

